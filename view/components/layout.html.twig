<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="css/output.css">
        {% block script %}{% endblock %}
        <title>
            {{ page_title }}
        </title>
    </head>
    <body>
        <nav class="bg-blue-400 text-white py-3 px-4 flex items-center justify-between">
            <a class="font-bold text-xl tracking-tight" href="#">Bibliothèque</a>
            <div class="flex items-center">
                <a class="text-sm px-4 py-2 leading-none rounded-full hover:bg-blue-500" href="/">Livres</a>
                <a class="text-sm px-4 py-2 leading-none rounded-full hover:bg-blue-500" href="#">Auteurs</a>
                <a class="text-sm px-4 py-2 leading-none rounded-full hover:bg-blue-500" href="#">Catégories</a>
                {% if session["connected"] == true %}
                    <a class="text-sm px-4 py-2 mr-8 leading-none rounded-full hover:bg-blue-500" href="/deconnection">Déconnection</a>
                    <img class="w-7.5" src="./img/user.png" alt="icone utilisateur">
                    <p>
                        {{ session["user"] }}</p>
                {% else %}
                    <a class="text-sm px-4 py-2 leading-none rounded-full hover:bg-blue-500" href="/connexion">Se connecter</a>
                {% endif %}
            </div>
        </nav>
        {% block content %}{% endblock %}
    </body>
</html>
